* 연산자 오버로딩 - 기본 연산자들을 직접 사용자가 정의하는 것을 연산자를 오버로딩(overloading), 기본 연산자를 여러분이 설계한 클래스에 맞게 직접 사용하는 것을 '연산자를 오버로딩 했다' 라고 부릅니다.
- 연산자 오버로딩을 알기 위해선 벡터 그리고 멤버 메서드에 대해서 알아야 한다.
- 연산자 오버로딩을 하기 위해선 개별적인 정의가 필요하다.

* 벡터(실수 : float, double)
(2) (5)         그림이 안그려져서 표현하기가 어렵지만, 두 수를 묶어 놓은 것이 벡터이다. 즉, 2와 7 이 하나의 벡터 5와 -3이 하나의 벡터이다. **백터가 두개인 것만은 아니다. 여러개가 올 수 있다.
(7) (-3)        그리고 연산 곱셈, 덧셈 등이 가능하며 곱하기에는 내적, 외적이 있다. 그래픽, 게임을 만드려는 사람은 무조건 벡터 개념을 알아야한다.
                또한 실수 와 벡터의 연산도 가능하다.

* 멤버 메서드
1. 멤버 메서드 선언, 정의 분리하기
- 선언과 정의를 분리한 다는 의미는 int a; int b; int c;를 선언해주고 a,b,c의 정의를 해준다는 의미이다. 즉, int a를 선언하고 바로 정의를 
해버리면 b,c에 대한 정의를 모르기 때문에 컴파일시 오류가 발생하기 때문이다.
2. 정적, 동적으로 분리하기

* 연산자 오버로딩
operate+
operate-
operate*(실수 * 벡터, 벡터 * 벡터)
operate/
리턴 타입 operate 연산자 연산자가 받는 인자
