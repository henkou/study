#include<iostream>
using namespace std;
int main() {
	int a, sum;
	sum = 0;
	cin >> a;
	for (int i = 1; i <= a; i++) {
		if (sum >= a) {
			break;
		}
		else {
			sum += i;
		}
	}
	cout << sum << endl;
}
