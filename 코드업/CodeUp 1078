#include<iostream>
using namespace std;
int main(){
    int a, sum;
    cin >> a;
    sum = 0;
    for(int i = 1; i <= a; i++){
        if(i % 2 == 0){
            sum += i;
        }
    }
    cout << sum << endl;
}
